input {
  tcp {
    port  => "5046"
    codec => json_lines
  }
}

output {
  stdout { codec => rubydebug }

  file {
    path  => "/usr/share/logstash/logs/%{[app]}.log"
    codec => json_lines
  }
}
