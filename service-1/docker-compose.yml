services:
  service-1:
    container_name: service-1
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "npm run start"
    env_file:
      - .env
    restart: always
    ports:
      - "3000:3000"
    networks:
      - labs-net

networks:
  labs-net:
    external: true
